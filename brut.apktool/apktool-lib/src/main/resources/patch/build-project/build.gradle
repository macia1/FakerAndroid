// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

allprojects {
    buildscript {
        repositories {
            google()
            jcenter()
            maven { url uri('https://dl.bintray.com/efaker/FakerAndroidGradle') }
        }

        dependencies {
            classpath 'com.android.tools.build:gradle:3.4.0'
            classpath 'com.faker.android:gradle:1.0.3'
        }
    }

    repositories {
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
